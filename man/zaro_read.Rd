% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{zaro_read}
\alias{zaro_read}
\title{Read data from a Zarr array}
\usage{
zaro_read(store, path, start = NULL, count = NULL, meta = NULL, verbose = TRUE)
}
\arguments{
\item{store}{a store object from \code{\link[=zaro]{zaro()}}}

\item{path}{character. Path to the array within the store.}

\item{start}{integer vector. 0-based start indices for each dimension.
Defaults to the origin.}

\item{count}{integer vector. Number of elements to read along each
dimension. Use \code{NA} for any axis to read the full extent from
\code{start}. Defaults to the full extent.}

\item{meta}{optional pre-fetched ZaroMeta object. If NULL (default),
metadata is read from the store. Can also be the root group metadata
from \code{zaro_meta(store)} — the array will be looked up by path
in the consolidated entries.}

\item{verbose}{logical. Emit diagnostic messages (default TRUE).}
}
\value{
An R array with dimensions matching \code{count}.
}
\description{
Reads a hyperslab from a Zarr array, fetching and decoding the necessary
chunks and assembling them into an R array. Supports both Zarr V2 and V3.
}
