% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chunk.R
\name{zaro_chunk_apply}
\alias{zaro_chunk_apply}
\title{Apply a function over chunks}
\usage{
zaro_chunk_apply(
  store,
  path,
  fun,
  ranges = NULL,
  meta = NULL,
  shaped = TRUE,
  parallel = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{store}{a store object from \code{\link[=zaro]{zaro()}}}

\item{path}{character. Path to the array.}

\item{fun}{function. Called with a single chunk result list (as from
\code{zaro_chunk()}). NULL chunks (missing) are skipped.}

\item{ranges}{optional range specification (as in \code{zaro_chunks()}).}

\item{meta}{optional pre-fetched ZaroMeta (or root group meta).}

\item{shaped}{logical. If TRUE, reshape each chunk's values.}

\item{parallel}{logical. Use future.apply for parallel execution.}

\item{verbose}{logical.}
}
\value{
A list of results from \code{fun}, one per non-NULL chunk.
}
\description{
Iterates over chunks and applies a function to each, accumulating
results. Chunks are fetched and decoded one at a time (or in parallel)
so memory usage is proportional to one chunk, not the full array.
}
\examples{
\dontrun{
# per-chunk mean, memory-efficient
means <- zaro_chunk_apply(store, "temperature", function(chunk) {
  mean(chunk$values, na.rm = TRUE)
})

# first time step only, all spatial chunks
means <- zaro_chunk_apply(store, "temperature", function(chunk) {
  c(mean = mean(chunk$values, na.rm = TRUE),
    n_valid = sum(is.finite(chunk$values)))
}, ranges = list(0, NULL, NULL))
}
}
